"""The four billed_api passes derive.{merge,entity,claim,report}.llm, reaching a model
through io.service(). Absent from the shipped [graph.passes] enable, therefore off.

Specified in 02-architecture.md section 2 row 45.
"""
