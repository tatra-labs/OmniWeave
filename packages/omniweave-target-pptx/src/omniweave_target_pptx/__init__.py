"""compile.pptx.native and parse.pptx.native; owsvg/1. Four target-owned artefacts:
constructs.toml, a RulePack of one-function-per-file predicates, lower() and
read_back(). The framework validates it; there is deliberately no validate() on
CompileV1.

Specified in 02-architecture.md section 2 row 46 and 09-generation.md.
"""
