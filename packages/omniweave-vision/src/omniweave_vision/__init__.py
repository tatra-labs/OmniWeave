"""parse.page.olmocr and nothing else — a derive.*.vlm pass is out of the release-1
ladder (ADR-8 D8.3). Reaches a model through io.service("vlm") across seam S3.

Specified in 02-architecture.md section 2 row 43.
"""
