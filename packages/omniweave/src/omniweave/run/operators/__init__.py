"""The Operators: one generic class per Port, next to the middleware that is their only caller.

08-runtime.md:1905-1912 places them here -- *"`operators/acquire.py · parse.py · derive.py ·
embed.py · compile.py` one per Port"* -- and `parse.py` is the first to land.
"""
